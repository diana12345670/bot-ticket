# Discord Bot Token (required)
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# PostgreSQL Database URL (optional - if not set, uses JSON file storage)
# DATABASE_URL=postgresql://user:password@host:5432/database

# OpenAI API Key (optional - for AI responses in tickets)
# OPENAI_API_KEY=your_openai_api_key_here

# Data directory for JSON storage (optional - defaults to ./data)
# DATA_DIR=./data

# Port for the web server (optional - defaults to 5000)
# PORT=5000

# Node environment (production for Railway)
NODE_ENV=production
